(()=>{const auth=window.CyberlawAuth,cfg=window.CYBERLAW_AUTH_USERS||{};if(!auth)return;const fnv=s=>{let h=0x811c9dc5;for(let i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,0x01000193)>>>0}return h>>>0};const form=document.getElementById('password-form'),msg=document.getElementById('password-message'),userEl=document.getElementById('account-name');if(userEl)userEl.textContent=auth.user;form?.addEventListener('submit',e=>{e.preventDefault();const old=document.getElementById('password-current').value,neo=document.getElementById('password-new').value,again=document.getElementById('password-confirm').value;const expected=Number(localStorage.getItem(auth.passwordKey)||0)||cfg[auth.user];if(fnv(`${auth.user}|${old}`)!==expected){msg.textContent='当前密码不正确。';msg.className='account-message error';return}if(neo.length<8){msg.textContent='新密码至少需要8位。';msg.className='account-message error';return}if(neo!==again){msg.textContent='两次输入的新密码不一致。';msg.className='account-message error';return}if(neo===old){msg.textContent='新密码不能与当前密码相同。';msg.className='account-message error';return}localStorage.setItem(auth.passwordKey,String(fnv(`${auth.user}|${neo}`)));msg.textContent='密码已修改。';msg.className='account-message success';form.reset()})})();