# 网信法研究每日推送：执行与交付

每日更新先阅读 `BRIEF_STRUCTURE.md`、本文件与 `.github/editions/README.md`，并读取最新 main、`data/current-edition.json` 和全部历史索引。不要把本文件当作已执行任务的证据。

全球科技法简报、域外法学论文精读、法学经典著作、科技法研究新作四栏由各自内容任务独立研究、核验和发布，不再以“四栏同日全部完成”为单栏发布前提。标准产出仍为18—20条新闻、1篇精读、1部经典、5项近两年新作。某栏完成并真实核验后，只推进该栏自己的当前指针、首页入口和历史列表；不得因为其他栏未完成、有问题或日期不同而阻塞，也不得借机改写其他栏。经典解读应显著长于论文精读，完整展开全书。保留全部历史正文及收藏、笔记、账户数据。

新闻发布时硬性72小时；发布前两小时内重扫重要来源。严禁旧闻补数、虚构书目、原题、页码或来源。核验状态、程序性质、外文原名与书目必须以查到的材料为依据，不能靠校验程序得分代替人工/研究代理查阅原文。

不要恢复“本期最值得先看”。读者页面不写制作说明。关联阅读排除图书，并且必须能够说明与当前材料的直接联系：同一事件或事件链、同一主体、同一具体法律文件或制度、同一执法/裁判问题、同一技术机制、同一受监管行为、同一明确法律争点，或者直接补充事实背景、制度背景或后续发展。仅仅同属“人工智能治理”“数据法”“平台监管”“个人信息保护”等宽泛领域，不构成关联；说不清“这篇材料具体帮助理解当前内容的哪一点”时就不收录，不因数量不足而凑项。

专题时间线采用明显更高的门槛，优先只收录同一法规、同一案件、同一企业/监管事件的连续发展，例如提案—通过—实施—执法，或调查—处罚—复议/诉讼—执行。确实不能按单一事件组织时，才允许使用高度同质且足够具体的窄专题，例如“中国个人信息保护行政处罚”，不得使用“个人信息保护”“数据治理”“AI监管”等宽泛主题。每个节点必须能解释为何属于同一事件链或同质专题，并让读者看出前后发展；平行但彼此无关的案例不得仅按日期排列成“时间线”。弱节点直接删除，删除后无法形成清楚脉络的专题直接取消。专题宁缺毋滥，最多保留少量真正成立的专题。

新闻条目另设与“关联阅读”“专题时间线”并列的“背景与前瞻”可选阅读区：背景信息只使用已核验来源或站内已核验历史解释制度、事件、技术或产业基础；未来前瞻分析可能产生的法律、监管、合规、产业或国际规则影响，必须使用条件化表达，不把推演写成事实。新新闻按 `BRIEF_STRUCTURE.md` 写入结构化 `brief-insight` 字段；历史正文冻结时只做安全回退，不反向补写正文。

外国新闻的原始英文或当地语言题名必须使用独立 `brief-original` 行，原始题名和新闻事实均全宽显示；其存在不得改变“法治研判｜智库选题参考｜论文选题”三栏位置。此项应纳入阅读器和响应式布局回归。

## 网信法技术基础教学

“网信法技术基础”是独立教学序列，不属于全球科技法简报、域外法学论文精读、法学经典著作、科技法研究新作四栏，也不得因四栏任一栏的完成状态而阻塞。入口固定为 `tech-basics.html`，每日课程放在 `tech-basics/` 目录。

教学对象是网络与信息法、人工智能法治及科技治理研究者。每课只解释一个核心技术问题，优先采用“今日问题—一句话结论—机制图/流程图—关键概念—常见误解—法治连接—研究延伸—原始材料”的轻量结构。图表必须承担解释功能，不使用纯装饰性图片；没有编程背景的读者应能在约5—8分钟内读完并形成准确技术直觉。

课程页面与课程索引的主导航均应保留“网信法技术基础”入口。新生成的当期四栏正文也应在 `.navlinks` 中加入该入口：根目录页面链接 `tech-basics.html`，`articles/`、`papers/`、`new-works/` 等一级子目录页面链接 `../tech-basics.html`。现有四栏发布、核验与历史索引逻辑不得因为教学序列而改变。

## 中断与续作

首先检查当天已存在的正文、来源记录和核验状态，只续作缺项，不重复写整期。某栏未完成时不得先改变该栏的 `data/current-edition.json` 指针、首页入口或归档；其他已经完成并核验通过的栏目不受其阻塞。不能在只提交几个文件后报告完成。

每栏真实核验完成后，按现行 current-columns / edition 记录保存核验状态和最终正文摘要。不得为了通过门槛把未检查的项目写成 true。正文在核验后又修改，必须重新核对并更新摘要；新闻跨过72小时或终检超过两小时，重新检索并替换过期新闻。

`每日推送发布保障` 只负责同步已经核验的栏目入口、导航、归档和当前栏目清单，显式触发 Pages，检查最终部署与线上入口；中断后读取 main 重试，不强推。它不会生成新闻，也不会代替来源核验。

原 ChatGPT 定时内容任务和仓库发布保障是不同执行者。不能因为仓库工作流成功就说 ChatGPT 定时任务配置已修改；不能把“pending/failed”说成“已上线”。只有最终对应提交 Pages completed/success 且本栏线上验收通过，才报告已发布。
